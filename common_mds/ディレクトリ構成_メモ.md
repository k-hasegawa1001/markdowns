\<myApp>\
 |-backend\
 | |-Dockerfile\
 |\
 |-fronend\
 |\
 |-proxy\
 | |-Dockerfile\
 |\
 |-db\
 |\
 |-redis\
 |\
 |-bff_web\
 | |-Dockerfile\
 |\
 |-bff_mobile\
 | |-Dockerfile\
 |\
 |-docker-compose.yml\
 |-docker-compose.override.yml\
 |-docker-compose.prod.yml\
 |-.env\
 |-.env.example\

上記システム構成は、モバイルの認証でFirebase Authを使用していない場合のみ\
Firebase Authを使用するならばredisや認証サーバーは不要

\
本番環境（docker-compose.prod.ymlを使用）での`compose up`コマンドは

```bash
docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d
```

になる
